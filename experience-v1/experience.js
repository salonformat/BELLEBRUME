const scenes=[...document.querySelectorAll('.scene')],root=document.querySelector('#experience'),sound=document.querySelector('#sound');let current=Math.min(4,Math.max(1,Number(new URLSearchParams(location.search).get('scene'))||1)),audio;
if(current!==1){scenes.forEach((scene,index)=>{const active=index===current-1;scene.classList.toggle('active',active);scene.setAttribute('aria-hidden',String(!active))})}
requestAnimationFrame(()=>setTimeout(()=>scenes[current-1].classList.add('ready'),180));
function show(n){const old=scenes[current-1],next=scenes[n-1];old.classList.remove('active','ready');old.setAttribute('aria-hidden','true');next.classList.add('active');next.setAttribute('aria-hidden','false');current=n;setTimeout(()=>next.classList.add('ready'),1400);localStorage.setItem('bellebrume-scene',String(n));soundCue(n)}
document.querySelectorAll('[data-next]').forEach(el=>el.addEventListener('click',()=>show(Number(el.dataset.next))));document.querySelector('.number').addEventListener('click',()=>{document.querySelector('.trace').classList.add('show');localStorage.setItem('bellebrume-trace','blue-flour')});document.querySelector('#restart').addEventListener('click',()=>{document.querySelector('.trace').classList.remove('show');show(1)});
root.addEventListener('pointermove',e=>{root.style.setProperty('--pointer-x',`${e.clientX}px`);root.style.setProperty('--pointer-y',`${e.clientY}px`)});
function beep(freq,delay=.01,pan=0){if(!audio)return;const t=audio.currentTime+delay,o=audio.createOscillator(),g=audio.createGain(),p=audio.createStereoPanner();o.frequency.value=freq;o.type='sine';p.pan.value=pan;g.gain.setValueAtTime(.0001,t);g.gain.exponentialRampToValueAtTime(.025,t+.03);g.gain.exponentialRampToValueAtTime(.0001,t+.55);o.connect(g).connect(p).connect(audio.destination);o.start(t);o.stop(t+.6)}function soundCue(n){if(!audio)return;if(n===2){beep(820,.25,.6);beep(1050,.38,.6)}if(n===3){beep(510,.2,-.4)}if(n===4){beep(330,.2,-.2);beep(440,.5,.3)}}sound.addEventListener('click',()=>{const on=sound.getAttribute('aria-pressed')!=='true';sound.setAttribute('aria-pressed',String(on));sound.textContent=on?'le village écoute':'écouter le village';if(on){audio??=new(window.AudioContext||window.webkitAudioContext)();audio.resume();soundCue(current)}else audio?.suspend()});
